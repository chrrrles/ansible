---
# defaults file for NAME_OF_VMWARE_MODULE
username: "{{ vmware_username|default('ansible_user') }}"
host: "{{ vmware_host|default('localhost') }}"
password: "{{ vmware_password|default('abc123Q%') }}"
